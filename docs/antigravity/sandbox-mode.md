# 沙箱化 (Sandboxing)

沙箱化为终端命令提供内核级隔离，目前在 macOS 上利用 "Seatbelt" (sandbox-exec)。

**限制：**

* **文件系统**：命令只能写入工作空间和必要的系统位置。
* **网络访问**：可以通过用户设置独立地允许或拒绝。

**处理违规：**

* 如果命令被阻止，您可以选择针对该特定命令绕过沙箱，或在设置中完全禁用沙箱化。

**集成：**
当启用安全模式时，沙箱化会自动激活并默认拒绝网络访问，以实现最大保护。
