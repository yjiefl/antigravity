# 浏览器子代理 (Browser Subagent)

当智能体需要与网络交互时，它会调用专业的浏览器子代理。

**能力：**

* 可使用点击、滚动、输入和读取控制台日志等工具。
* 能够通过 DOM 捕获、截图或 Markdown 解析来读取页面。
* 子代理运行专门为 Web 操作优化的模型，这与主智能体模型不同。

**用户交互：**

* 当子代理处于活动状态时，会出现蓝色边框和操作说明。
* 子代理工作时，用户被限制与页面交互，以防混淆。
* 子代理可以在后台标签页中工作，允许用户同时使用其他标签页。
