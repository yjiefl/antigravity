# 安全模式 (Secure Mode)

安全模式通过限制智能体对外部资源和敏感操作的访问，提供增强的安全控制。

**特性：**
* **URL 允许/拒绝列表**：管理与外部网站的交互，包括图像渲染和“读取 URL”工具。
* **审查策略**：
    * **终端**：所有命令都需要审查；忽略允许列表。
    * **浏览器 JS**：执行时始终提示权限。
    * **产物**：在对计划采取行动之前始终提示确认。
* **文件系统访问**：
    * 尊重 `.gitignore`。
    * 仅限访问指定的工作空间（禁用工作空间以外的访问）。
